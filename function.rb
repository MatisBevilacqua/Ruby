# Ce code permet de déclarer une fonction, ( def ) permet de declarer un fonction.
# ( def ) remplace en javascript ( function), et end pour déclarer la fin de la fonction. 
# Ensuite pour l'éxécuter, rien de plus simple. Il faut juste appeller le nom de la fonction.

name = 'Hello'

def test (n) 
    puts "#{n} World"
end

test(name)

